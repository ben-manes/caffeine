# Configuration for the zizmor static analysis tool
# https://woodruffw.github.io/zizmor/configuration/
rules:
  dangerous-triggers:
    ignore:
      - dependency-submission-pr-retrieve.yml
  secrets-outside-env:
    disable: true
  unpinned-images:
    ignore:
      - semgrep.yml
      - snyk.yml
